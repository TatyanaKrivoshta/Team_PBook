<Window x:Class="PBook_UI.EditContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Редактирование контакта" Height="420" Width="400"
        WindowStartupLocation="CenterScreen">
        

    <Window.Resources>
        <Style TargetType="Label" x:Key="FieldLabelStyle">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style TargetType="TextBox" x:Key="FieldTextStyle">
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style TargetType="ComboBox" x:Key="FieldComboTextStyle">
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Фамилия -->
        <Label Content="Фамилия:" Style="{StaticResource FieldLabelStyle}" Grid.Row="0" />
        <TextBox Text="{Binding Contact.Person.LastName, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource FieldTextStyle}" Grid.Row="1" />

        <!-- Имя -->
        <Label Content="Имя:" Style="{StaticResource FieldLabelStyle}" Grid.Row="2" />
        <TextBox Text="{Binding Contact.Person.FirstName, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource FieldTextStyle}" Grid.Row="3" />

        <!-- Отчество -->
        <Label Content="Отчество:" Style="{StaticResource FieldLabelStyle}" Grid.Row="4" />
        <TextBox Text="{Binding Contact.Person.Patronymic, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource FieldTextStyle}" Grid.Row="5" />

        <!-- Телефон -->
        <Label Content="Телефон:" Style="{StaticResource FieldLabelStyle}" Grid.Row="6" />
        <TextBox Text="{Binding Contact.Number, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource FieldTextStyle}" Grid.Row="7" />

        <!-- Тип телефона -->
        <Label Content="Тип телефона:" Style="{StaticResource FieldLabelStyle}" Grid.Row="8" />
        <ComboBox ItemsSource="{Binding PhoneTypes}"
                  SelectedItem="{Binding SelectedType, UpdateSourceTrigger=PropertyChanged}"
                  DisplayMemberPath="Type"
                  Style="{StaticResource FieldComboTextStyle}"
                  Grid.Row="9" />

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="10" Margin="5">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="100" Margin="5" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Margin="5" />
        </StackPanel>
    </Grid>
</Window>